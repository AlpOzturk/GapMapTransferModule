For transfer of data. Both azure and starford side.

IMPORTANT NOTES:

- SQL alchemy stores all strings as unicode, which may lead to a UnicodeDecodeError if retrieved and processes without considering that.
